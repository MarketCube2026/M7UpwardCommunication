import { NextResponse } from "next/server";
import { db } from "@/lib/db";
export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { const { id } = await context.params; const body = await request.json(); if (!String(body.outcome ?? "").trim()) return NextResponse.json({ error: "请填写实际结果" }, { status: 400 }); const item = await db.debrief.upsert({ where: { rehearsalId: id }, update: { outcome: String(body.outcome), rating: body.rating ? Number(body.rating) : null, variance: body.variance ? String(body.variance) : null, nextAction: body.nextAction ? String(body.nextAction) : null }, create: { rehearsalId: id, outcome: String(body.outcome), rating: body.rating ? Number(body.rating) : null, variance: body.variance ? String(body.variance) : null, nextAction: body.nextAction ? String(body.nextAction) : null } }); return NextResponse.json(item); }
