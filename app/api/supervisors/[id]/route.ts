import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { supervisorSchema } from "@/lib/schemas";
import { requireUser } from "@/lib/http";
type Context = { params: Promise<{ id: string }> };
export async function GET(_: Request, context: Context) { const auth=await requireUser(); if("response" in auth)return auth.response; const {id}=await context.params; const item=await db.supervisor.findFirst({where:{id,userId:auth.user.id},include:{rehearsals:{where:{userId:auth.user.id},orderBy:{createdAt:"desc"},take:10}}}); return item?NextResponse.json(item):NextResponse.json({error:"未找到档案"},{status:404}); }
export async function PATCH(request: Request, context: Context) { const auth=await requireUser(); if("response" in auth)return auth.response; const {id}=await context.params; const existing=await db.supervisor.findFirst({where:{id,userId:auth.user.id}}); if(!existing)return NextResponse.json({error:"未找到档案"},{status:404}); const parsed=supervisorSchema.safeParse(await request.json()); if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message??"输入无效"},{status:400}); const data=parsed.data; return NextResponse.json(await db.supervisor.update({where:{id},data:{...data,tags:JSON.stringify(data.tags),communicationPrefs:JSON.stringify(data.communicationPrefs),workStyle:JSON.stringify(data.workStyle)}})); }
export async function DELETE(_: Request, context: Context) { const auth=await requireUser(); if("response" in auth)return auth.response; const {id}=await context.params; const result=await db.supervisor.deleteMany({where:{id,userId:auth.user.id}}); return result.count?NextResponse.json({ok:true}):NextResponse.json({error:"未找到档案"},{status:404}); }
